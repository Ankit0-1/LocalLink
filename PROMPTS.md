# Project Working Instructions

Read every Markdown file before changing the specification. Implement one scoped task at a time, explain material design decisions, and update `TASKS.md` after completing a task.

Preserve the marketplace architecture: vendors own stores; stores own products; orders are single-store and fulfilled only by the owning vendor before delivery assignment. If requirements are uncertain, ask before changing completed work.
